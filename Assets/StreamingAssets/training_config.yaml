# Конфиг параметров обучения.
#
# Переопределяет публичные поля компонентов значениями отсюда — не нужно
# открывать Unity/пересобирать билд, чтобы поменять параметр между запусками
# headless-обучения (mlagents-learn --env=Build.exe). Читается один раз при
# старте, до генерации первой арены.
#
# Формат: секция — имя класса компонента, ключ — точное имя его публичного
# поля (как в инспекторе, но без пробелов, camelCase). Поддерживаются числа
# (float/int), true/false, строки и пары вида [x, y] для Vector2/Vector2Int.
#
# Всё ниже закомментировано — файл сейчас ничего не переопределяет, значения
# из инспектора действуют как обычно. Раскомментируйте и поправьте нужные
# строки (снимите "#" в начале строки).

# RobotBrain:
#   gripReward: 1.0
#   outOfBoundsPenalty: -0.5
#   stepPenalty: -0.0002
#   wallPenalty: -0.01
#   moveRewardScale: 0.01
#   fallDistance: 3
#   arenaEscapeMargin: 2
#   noDetectionSteps: 0
#   grabAttemptReward: 0.3
#   grabZoneRadius: 0.75
#   smoothnessPenaltyScale: 0.002
#   cameraRewardScale: 0.02
#   cameraAngleDeadZone: 0.05
#   cameraRewardClamp: 0.02
#   wallProximityPenalty: 0
#   collisionPenaltyCooldown: 0.5
#   failedGrabPenalty: -0.1
#   speedPenaltyThresholdLinear: 0.2
#   speedPenaltyThresholdAngular: 30
#   speedPenaltyAmount: -0.05
#   arenaHalfExtents: [300, 300]
#   spawnHalfExtents: [15, 15]
#   randomizeSpawn: true
#   randomizeBall: true
#   minSpawnDistance: 2
#   isTraining: true
#   regenerateEveryEpisodes: 10

# EnvironmentManager:
#   globalScale: 10
#   baseArenaSize: [3, 6]
#   boxCount: 10
#   baseSafeRadius: 0.3
#   scatterMargin: 0.5
#   maxBallDistanceFromFarWall: 1.15
#   createWalls: true
#   visibleWalls: true
#   baseWallHeight: 0.5

# ArenaSpawner:
#   arenaCount: 20
#   spacing: 100
#   arenasPerRow: 4
#   floorMargin: 1.2

# TrackController:
#   moveSpeed: 3
#   turnSpeed: 120
#   turnK: 0.3
#   maxLinearCmd: 0.25
#   motorDeadzone: 10
#   minMotorPwm: 35
#   pwmScale: 200

# VirtualSensors:
#   # Левый и правый ИК: 2.4 единицы = 24 см.
#   irDistance: 2.4
#   # Центральный ИК физически перенесён на захват.
#   gripperDistance: 0.7
#   ultrasonicMaxDistance: 50
#   ultrasonicRayCount: 10
#   ultrasonicAngle: 45

# SimulatedYoloCamera:
#   maxVisibleDistance: 30
#   horizontalFOV: 40
#   cameraAspectRatio: 1.3333333
#   randomizeHorizontalFOV: true
#   horizontalFOVRange: [35, 55]
#   verticalTiltRange: [-5, 5]
#   randomizeSensorEffects: true
#   angleNoiseStdDevRange: [0, 0.03]
#   detectionDropoutRange: [0, 0.1]
#   latencyMeasurementsRange: [0, 2]
